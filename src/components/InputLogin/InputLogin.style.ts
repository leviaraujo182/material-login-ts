import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles({
    input: {
        width: '90%',
        borderRadius: 10,
        marginTop: '10px',
    },

    container_input: {
        display: 'flex',
        justifyContent: 'Center'
    }
})